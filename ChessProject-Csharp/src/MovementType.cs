namespace SolarWinds.MSP.Chess
{
    /// <summary>
    /// Movement type enum. Move or Capture
    /// </summary>
    public enum MovementType
    {
        Move = 0,
        Capture = 1
    }
}
